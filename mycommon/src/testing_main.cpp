#include "http_helper.h"
#include "config.h"
#include <functional>

// Properties config;
int main([[maybe_unused]] int argc, [[maybe_unused]] char *argv[])
{
    setbuf(stdout, NULL);
    return 0;
}